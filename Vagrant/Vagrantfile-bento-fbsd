IMAGE_NAME = "freebsd/FreeBSD-14.1-STABLE"

Vagrant.configure("2") do |config|
    config.ssh.insert_key = false

    config.vm.provider "virtualbox " do |v|
        v.allowlist_verified = true
        v.memory = 1024
        v.cpus = 1
    end

    config.vm.define "fbsd" do |fbsd|
        fbsd.vm.box = IMAGE_NAME
        fbsd.vm.network "forwarded_port", guest: 22, host: 2211, host_ip: "127.0.0.1"
	fbsd.vm.hostname = "fbsd14"
    end

end
